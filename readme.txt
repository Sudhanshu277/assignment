1. Import the data(database) in your system after that run the application in your system in localhost serve
2. After runing the application you will get the option for import you can choose the file and import on the database
3. You will see the table in which data will be showing (if you have import the database) you can export the data on clicking the button Export.